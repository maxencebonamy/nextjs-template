import type { PropsWithChildren, ReactNode } from "react"

const {{pascalCase name}}Layout = ({ children }: PropsWithChildren): ReactNode => {
	return (
		<>
			{ children }
		</>
	)
}

export default {{pascalCase name}}Layout
